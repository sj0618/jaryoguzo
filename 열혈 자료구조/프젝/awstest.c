/* i want to make phone number book by struct which have name,number,address */
#include <stdio.h>
#include <string.h>
#define MAX_NAME_SIZE 20
struct PB
{
    char name[MAX_NAME_SIZE];
    char number[MAX_NAME_SIZE];
    char address[MAX_NAME_SIZE];
}pbn;
 s ={'a','a','a'};
(s).name[0]='a';